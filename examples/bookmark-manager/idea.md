# Idea: Bookmark Manager

> I want to build a free AI-powered bookmark manager with web app, PWA, browser extension, semantic search, and optional self-hosting.

This is the raw idea, verbatim, from `ProjectFounder-idea.md` § 123 Step 1 ("Concrete v0.1 User Journey"). It's used as the input to `workflows/new-project.md` in this example — see `PROJECT.yaml` in this directory for the captured/classified output, and `NOTES.md` for what running the workflow against it actually surfaced.
