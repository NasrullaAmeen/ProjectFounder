# Idea: Extend an existing notes app

> I already have a working open-source Markdown note-taking app, self-hosted. I want to extend it with AI-powered auto-tagging and a public API for third-party integrations, without migrating off it or rebuilding it from scratch.

This is a deliberately non-`CREATE` idea, invented to exercise `workflows/new-project.md`'s intent classification and the § 163.2 Explore branch (`docs/TASKS.md`: stress-test Phase 0 against a second, non-greenfield example). See `PROJECT.yaml` for the captured/classified output and `NOTES.md` for what running the workflow surfaced.
