# Examples

Worked examples, including the canonical v0.1 user journey (see ProjectFounder-idea.md, Section 123 — Concrete v0.1 User Journey): a free AI-powered bookmark manager with web app, PWA, browser extension, and semantic search.

- [`bookmark-manager/`](./bookmark-manager/) — § 123's idea, `intent: CREATE`, taken furthest of any example: `workflows/new-project.md` (Phase 0, capture + classify) → `workflows/brainstorm.md` (Discovery + Brainstorm) → `workflows/requirements.md` (Gap Analysis + Feature Discovery + Requirements), lifecycle `CLASSIFIED` → `DISCOVERY` → `SCOPED`. See `bookmark-manager/NOTES.md` for three rounds of findings — a taxonomy gap (§ 163.5), a missing "ask the human" step in Discovery, and a missing lifecycle state for scoping ahead of Research (§ 163.14).
- [`notes-app-extend/`](./notes-app-extend/) — an invented non-`CREATE` idea (`intent: EXTEND`), run through `workflows/new-project.md` specifically to exercise the § 163.2 Explore branch. See `notes-app-extend/NOTES.md` — found that § 25's lifecycle had no state for § 163.2's Explore step at all, fixed by § 163.12.
- [`ambiguous-intent/`](./ambiguous-intent/) — an invented idea that plausibly fits three of § 27's intent values at once (`REBUILD`, `MIGRATE`, `EXTEND`), run through `workflows/new-project.md` specifically to stress-test intent classification once it's no longer a clean single answer. See `ambiguous-intent/NOTES.md` — found the escalation contract had no rule for intent ambiguity (only for `types` ambiguity), fixed by § 163.13.

Status: `bookmark-manager/` exercises the full Phase 1 pipeline built so far; `notes-app-extend/` and `ambiguous-intent/` stop at Classification (non-`CREATE` intents — Discovery refuses to run against them). § 123's Research step onward (Phase 2+) and § 163.2's Explore step aren't run anywhere yet since those engines don't exist.
