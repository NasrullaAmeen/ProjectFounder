# Templates: core

Document/artifact templates for the core domain.

- [`PROJECT.yaml`](./PROJECT.yaml) — fillable § 120 Project Manifest template, used by `workflows/new-project.md`. Validate against `schemas/project.schema.yaml`.

Status: partially populated (Phase 0). Other core templates (idea.md, README.md for a generated project, etc.) not yet added.
